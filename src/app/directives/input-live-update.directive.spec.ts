import { InputLiveUpdateDirective } from './input-live-update.directive';

describe('InputLiveUpdateDirective', () => {
  it('should create an instance', () => {
    const directive = new InputLiveUpdateDirective();
    expect(directive).toBeTruthy();
  });
});
